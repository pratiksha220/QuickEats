specVersion: "v1beta"
serviceId: "mini"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "mini-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
